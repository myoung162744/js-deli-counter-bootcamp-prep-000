var katzDeli = [];

function takeANumber(katzDeliLine, name) {
  katzDeliLine.push(name)
  var greeting = "Welcome, " + name +". You are number " + length.katzDeliLine" " in line."
  return greeting
}